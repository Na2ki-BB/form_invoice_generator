export type Product = {
  id: string
  name: string
  unitPrice: number
  quantity: number
}

export type CustomerInfo = {
  customerName: string
  customerKana: string
  postalCode: string
  address: string
  phone: string
  email: string
  note: string
}
